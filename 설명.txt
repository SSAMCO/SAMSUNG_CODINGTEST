깃허브 사이트 = 원격 저장소 
각자 컴퓨터 = 지역 저장소(로컬)

1. 로컬에 git clone을 이용해서 원격 저장소의 main을 가져옴
2. 수정할 사항을 진행
3. 새로운 브랜치를 만들어서 pr을 진행
(git add > git commit > git push)
4. pr을 진행하면 레포지토리에 포함된 유저들이 approve를 진행하고 모두 수락하면 main에 merge를 하게 됨
